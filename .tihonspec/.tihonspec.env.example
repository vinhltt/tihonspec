# TihonSpec Configuration Example
# Copy this file to .tihonspec.env and customize for your project

# ============================================================
# REQUIRED CONFIGURATION
# ============================================================

# Allowed ticket prefixes (comma-separated, case-insensitive)
# Example: "aa,BB,PROJ,hotfix"
TIHONSPEC_PREFIX_LIST="aa"

# Default folder when not specified in ticket ID
# Example: "feature", "tickets", "tasks"
TIHONSPEC_DEFAULT_FOLDER="feature"

# Main branch name for base
# Example: "master", "main", "develop"
TIHONSPEC_MAIN_BRANCH="master"

# Root directory for specs
# Example: ".tihonspec", ".spec", "specs"
TIHONSPEC_SPECS_ROOT=".tihonspec"

# ============================================================
# OPTIONAL CONFIGURATION
# ============================================================

# Custom ticket format validation regex
# Default: ^([a-zA-Z]+/)?([a-zA-Z]+)-([0-9]+)$
# TIHONSPEC_TICKET_FORMAT="^([a-zA-Z]+/)?([a-zA-Z]+)-([0-9]+)$"

# Validation hook script path (relative to repo root)
# Example: ".tihonspec/hooks/validate-ticket.sh"
# TIHONSPEC_VALIDATION_HOOK=""

# Hook timeout in seconds
# Default: 30
# TIHONSPEC_HOOK_TIMEOUT="30"

# Hook failure behavior: "exit" or "warn"
# Default: "exit"
# TIHONSPEC_HOOK_FAIL_BEHAVIOR="exit"

# ============================================================
# USAGE EXAMPLES
# ============================================================

# Default environment:
#   ./create-new-feature.sh aa-123 "Feature name"

# Specific environment:
#   TIHONSPEC_ENV=production ./create-new-feature.sh hotfix-999 "Fix"

# Debug mode:
#   TIHONSPEC_DEBUG=true ./create-new-feature.sh aa-456 "Test"

# ============================================================
# NOTES
# ============================================================

# 1. All TIHONSPEC_* variables are automatically loaded
# 2. Environment-specific configs: .feature.env.{environment}
# 3. Validation hooks receive these variables:
#    - TIHONSPEC_HOOK_PREFIX
#    - TIHONSPEC_HOOK_NUMBER
#    - TIHONSPEC_HOOK_FOLDER
#    - TIHONSPEC_HOOK_PHASE
#    - TIHONSPEC_HOOK_TICKET_ID
